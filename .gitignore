.claude/
.agents/
CLAUDE.local.md
node_modules/
.DS_Store
site/assets/pragmata/
site-dist/
.envrc
dist
dist-*
cabal-dev
.direnv
cabal.project.local
result
result-*
.pre-commit-config.yaml
db/
.mina/
.codex/

# keiro-dsl scaffold manifest artifacts (informational; emitted into --out)
keiro-dsl-manifest.*.txt

# Scratch benchmark baselines captured before code changes; committed baselines
# live under keiro/bench/baseline-*.csv after the implementation is complete.
keiro/bench-before-*.csv

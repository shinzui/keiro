-- @generated by keiro-dsl; do not edit. Regenerated from the .kdsl spec.
module Generated.HospitalCapacity.Hospital.EventStream
  ( hospitalEventStream
  , HospitalEventStream
  ) where

import Generated.HospitalCapacity.Hospital.Domain
import Generated.HospitalCapacity.Hospital.Codec (hospitalCodec)
import HospitalCapacity.Hospital.Holes (hospitalTransducer)
import Keiki.Core (HsPred)
import Keiro.EventStream (EventStream (..), SnapshotPolicy (..))
import qualified Keiro.Stream as Stream

type HospitalEventStream =
  EventStream (HsPred HospitalRegs HospitalCommand) HospitalRegs HospitalVertex HospitalCommand HospitalEvent

hospitalEventStream :: HospitalEventStream
hospitalEventStream =
  EventStream
    { transducer = hospitalTransducer
    , initialState = HospitalOperational
    , initialRegisters = initialHospitalRegs
    , eventCodec = hospitalCodec
    , resolveStreamName = Stream.streamName
    , snapshotPolicy = Never
    , stateCodec = Nothing
    }
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE OverloadedStrings #-}
-- @generated by keiro-dsl; do not edit. Regenerated from the .kdsl spec.
module Generated.HospitalCapacity.Surge.Codec
  ( surgeCodec
  , parseSurgeEvent
  , encodeSurgeEvent
  ) where

import Generated.HospitalCapacity.Surge.Domain
import Data.Aeson (Value, object, withObject, (.:), (.=))
import Data.Aeson.Types (Parser, parseEither)
import Data.List.NonEmpty (NonEmpty (..))
import Data.Text (Text)
import qualified Data.Text as T
import Keiro.Codec (Codec (..))




surgeCodec :: Codec SurgeEvent
surgeCodec =
  Codec
    { eventTypes = error "no events"
    , eventType = \case
    , schemaVersion = 1
    , encode = encodeSurgeEvent
    , decode = parseSurgeEvent
    , upcasters = []
    }

encodeSurgeEvent :: SurgeEvent -> Value
encodeSurgeEvent = \case

parseSurgeEvent :: Value -> Either Text SurgeEvent
parseSurgeEvent = mapLeftText . parseEither (withObject "SurgeEvent" go)
  where
    go o = do
      kind <- o .: "kind" :: Parser Text
      case kind of
        _ -> fail "unknown event kind"

mapLeftText :: Either String b -> Either Text b
mapLeftText = either (Left . T.pack) Right
{-# LANGUAGE OverloadedRecordDot #-}
-- @generated by keiro-dsl 0.14.0.0 (language keiro-dsl 4) from aggregate Page; do not edit.
module Generated.IncidentPaging.Page.Codec (
    pageCodec,
    parsePageEvent,
    encodePageEvent,
) where

import Generated.IncidentPaging.Page.Domain
import Data.Aeson (Value, object, withObject, (.:), (.=))
import Data.Aeson.Types (parseEither)
import Data.List.NonEmpty (NonEmpty (..))
import Data.List.NonEmpty qualified as NonEmpty
import Data.Text (Text)
import qualified Data.Text as T
import Keiro.Codec (Codec (..), EventType (..))





pageEventTypes :: NonEmpty EventType
pageEventTypes = EventType "PageSent" :| []

pageCodec :: Codec PageEvent
pageCodec =
  Codec
    { eventTypes = pageEventTypes
    , eventType = \case
        PageSent{} -> EventType "PageSent"
    , schemaVersion = 1
    , encode = encodePageEvent
    , decode = parsePageEvent
    , upcasters = []
    }

encodePageEvent :: PageEvent -> Value
encodePageEvent = \case
  PageSent payload ->
    object
      [ "kind" .= ("PageSent" :: Text)
      , "incidentId" .= payload.incidentId
      , "responderId" .= payload.responderId
      ]

parsePageEvent :: EventType -> Value -> Either Text PageEvent
parsePageEvent (EventType tag) = mapLeftText . parseEither (withObject "PageEvent" go)
  where
    go o = do
      case tag of
        "PageSent" ->
          PageSent
            <$> ( PageSentData
                    <$> o .: "incidentId"
                    <*> o .: "responderId"
                )
        _ -> fail ("unknown event type " <> show tag <> "; expected one of: " <> renderExpectedEventTypes pageEventTypes)

mapLeftText :: Either String b -> Either Text b
mapLeftText = either (Left . T.pack) Right

renderExpectedEventTypes :: NonEmpty EventType -> String
renderExpectedEventTypes =
  T.unpack
    . T.intercalate ", "
    . map (\(EventType eventTypeName) -> eventTypeName)
    . NonEmpty.toList
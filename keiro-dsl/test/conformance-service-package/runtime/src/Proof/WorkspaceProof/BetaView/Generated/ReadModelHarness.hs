{-# LANGUAGE OverloadedRecordDot #-}
-- @generated by keiro-dsl 0.11.0.0 (language keiro-dsl 4) from readmodel beta_view; do not edit.
module Proof.WorkspaceProof.BetaView.Generated.ReadModelHarness (readModelFacts, readModelFactResults, runReadModelFacts) where

import Proof.WorkspaceProof.BetaView.Generated.ReadModel (betaViewReadModel, betaViewAsyncProjection)
import Data.Text qualified as T
import Keiro.ReadModel (ReadModel (..), StrongScope (..))
import Keiro.Projection (AsyncProjection (..))

-- | (fact, expected from notation, actual generated runtime value).
readModelFacts :: [(String, String, String)]
readModelFacts =
  [ ("registryName", "workspace-proof-beta-view", T.unpack betaViewReadModel.name)
  , ("subscriptionName", "workspace-proof-beta-view-sub", T.unpack betaViewReadModel.subscriptionName)
  , ("shapeHash", "fnv1a:b20d797cf7a278b5", T.unpack betaViewReadModel.shapeHash)
  , ("asyncProjectionName", "workspace-proof-beta-view-async", T.unpack betaViewAsyncProjection.name)
  , ("consistency", "Eventual", show betaViewReadModel.defaultConsistency)
  , ("strongScope", "EntireLog", renderStrongScope betaViewReadModel.strongScope)
  ]

renderStrongScope :: StrongScope -> String
renderStrongScope EntireLog = "EntireLog"
renderStrongScope (CategoryHead categoryName) = "CategoryHead " <> T.unpack categoryName

readModelFactResults :: [(String, Bool)]
readModelFactResults =
  [(fact, expected == actual) | (fact, expected, actual) <- readModelFacts]

runReadModelFacts :: IO Bool
runReadModelFacts = do
  let failures = [(fact, expected, actual) | (fact, expected, actual) <- readModelFacts, expected /= actual]
  mapM_ (\(fact, expected, actual) -> putStrLn ("FAIL  " <> fact <> " expected=" <> show expected <> " actual=" <> show actual)) failures
  pure (null failures)
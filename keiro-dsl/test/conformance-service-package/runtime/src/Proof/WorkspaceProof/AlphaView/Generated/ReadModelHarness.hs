{-# LANGUAGE OverloadedRecordDot #-}
-- @generated by keiro-dsl 0.13.0.0 (language keiro-dsl 4) from readmodel alpha_view; do not edit.
module Proof.WorkspaceProof.AlphaView.Generated.ReadModelHarness (readModelFacts, readModelFactResults, runReadModelFacts) where

import Proof.WorkspaceProof.AlphaView.Generated.ReadModel (alphaViewReadModel, alphaViewAsyncProjection)
import Data.Text qualified as T
import Keiro.ReadModel (ReadModel (..), StrongScope (..))
import Keiro.Projection (AsyncProjection (..))

-- | (fact, expected from notation, actual generated runtime value).
readModelFacts :: [(String, String, String)]
readModelFacts =
  [ ("registryName", "workspace-proof-alpha-view", T.unpack alphaViewReadModel.name)
  , ("subscriptionName", "workspace-proof-alpha-view-sub", T.unpack alphaViewReadModel.subscriptionName)
  , ("shapeHash", "fnv1a:ebb780a44ff297c1", T.unpack alphaViewReadModel.shapeHash)
  , ("asyncProjectionName", "workspace-proof-alpha-view-async", T.unpack alphaViewAsyncProjection.name)
  , ("consistency", "Eventual", show alphaViewReadModel.defaultConsistency)
  , ("strongScope", "EntireLog", renderStrongScope alphaViewReadModel.strongScope)
  ]

renderStrongScope :: StrongScope -> String
renderStrongScope EntireLog = "EntireLog"
renderStrongScope (CategoryHead categoryName) = "CategoryHead " <> T.unpack categoryName

readModelFactResults :: [(String, Bool)]
readModelFactResults =
  [(fact, expected == actual) | (fact, expected, actual) <- readModelFacts]

runReadModelFacts :: IO Bool
runReadModelFacts = do
  let failures = [(fact, expected, actual) | (fact, expected, actual) <- readModelFacts, expected /= actual]
  mapM_ (\(fact, expected, actual) -> putStrLn ("FAIL  " <> fact <> " expected=" <> show expected <> " actual=" <> show actual)) failures
  pure (null failures)
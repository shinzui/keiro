-- @generated by keiro-dsl 0.12.0.0 (language keiro-dsl 4) from aggregate Alpha; do not edit.
module Proof.WorkspaceProof.Alpha.Generated.EventStream
  ( alphaCategory
  , alphaCommandCategory
  , alphaEventStream
  , alphaEventStreamDef
  , AlphaEventStream
  , AlphaEventStreamDef
  ) where

import Proof.WorkspaceProof.Alpha.Generated.Domain
import Proof.WorkspaceProof.Alpha.Generated.Codec (alphaCodec)
import Proof.WorkspaceProof.Alpha.Generated.Transducer (alphaTransducer)
import Keiki.Core (HsPred)
import Keiro.EventStream (EventStream (..), SnapshotPolicy (..))
import Keiro.EventStream.Validate (ValidatedEventStream, mkEventStreamOrThrow)
import Keiro.Stream qualified as Stream

-- The validated aggregate stream category (hole-kind 5: referenced, never retyped).
-- Entity streams are '<category>-<id>' via Keiro.Stream.entityStream.
-- categoryUnsafe is safe here because this generated literal passed the DSL category proof.
alphaCategory :: Stream.StreamCategory AlphaEventStreamDef
alphaCategory = Stream.categoryUnsafe "alpha"

-- The same category text, typed for command envelopes such as PMCommand.
alphaCommandCategory :: Stream.StreamCategory AlphaCommand
alphaCommandCategory = Stream.categoryUnsafe "alpha"

type AlphaEventStreamDef =
  EventStream (HsPred AlphaRegs AlphaCommand) AlphaRegs AlphaVertex AlphaCommand AlphaEvent

type AlphaEventStream =
  ValidatedEventStream (HsPred AlphaRegs AlphaCommand) AlphaRegs AlphaVertex AlphaCommand AlphaEvent

alphaEventStreamDef :: AlphaEventStreamDef
alphaEventStreamDef =
  EventStream
    { transducer = alphaTransducer,
      initialState = AlphaActive,
      initialRegisters = initialAlphaRegs,
      eventCodec = alphaCodec,
      resolveStreamName = Stream.streamName,
      snapshotPolicy = Never,
      stateCodec = Nothing
    }

alphaEventStream :: AlphaEventStream
alphaEventStream =
  mkEventStreamOrThrow "Alpha" alphaEventStreamDef
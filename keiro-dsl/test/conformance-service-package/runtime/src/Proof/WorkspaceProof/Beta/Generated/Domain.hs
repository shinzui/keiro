{-# LANGUAGE DuplicateRecordFields #-}
{-# LANGUAGE TemplateHaskell #-}
-- @generated by keiro-dsl 0.10.0.0 (language keiro-dsl 4) from aggregate Beta; do not edit.
module Proof.WorkspaceProof.Beta.Generated.Domain where

import Data.Proxy (Proxy (..))
import Data.Text (Text)
import GHC.Generics (Generic)
import Keiki.Core (RegFile (..))
import Proof.WorkspaceProof.Generated.Nominals (ProofId, parseProofId)
import Keiki.Generics.TH (deriveAggregateCtorsAll, deriveWireCtorsAll)

data BetaVertex = BetaActive
  deriving stock (Generic, Eq, Ord, Show, Enum, Bounded)

data PingBetaData = PingBetaData
  { proofId :: !ProofId
  }
  deriving stock (Generic, Eq, Show)

data BetaCommand = PingBeta !PingBetaData
  deriving stock (Generic, Eq, Show)

data BetaPingedData = BetaPingedData
  { proofId :: !ProofId
  }
  deriving stock (Generic, Eq, Show)

data BetaEvent = BetaPinged !BetaPingedData
  deriving stock (Generic, Eq, Show)

type BetaRegs =
  '[]

initialBetaRegs :: RegFile BetaRegs
initialBetaRegs =
  RNil

$(deriveAggregateCtorsAll ''BetaCommand ''BetaRegs)



$(deriveWireCtorsAll ''BetaEvent)
-- @generated by keiro-dsl 0.11.0.0 (language keiro-dsl 4) from aggregate Beta; do not edit.
module Proof.WorkspaceProof.Beta.Generated.EventStream
  ( betaCategory
  , betaCommandCategory
  , betaEventStream
  , betaEventStreamDef
  , BetaEventStream
  , BetaEventStreamDef
  ) where

import Proof.WorkspaceProof.Beta.Generated.Domain
import Proof.WorkspaceProof.Beta.Generated.Codec (betaCodec)
import Proof.WorkspaceProof.Beta.Generated.Transducer (betaTransducer)
import Keiki.Core (HsPred)
import Keiro.EventStream (EventStream (..), SnapshotPolicy (..))
import Keiro.EventStream.Validate (ValidatedEventStream, mkEventStreamOrThrow)
import Keiro.Stream qualified as Stream

-- The validated aggregate stream category (hole-kind 5: referenced, never retyped).
-- Entity streams are '<category>-<id>' via Keiro.Stream.entityStream.
-- categoryUnsafe is safe here because this generated literal passed the DSL category proof.
betaCategory :: Stream.StreamCategory BetaEventStreamDef
betaCategory = Stream.categoryUnsafe "beta"

-- The same category text, typed for command envelopes such as PMCommand.
betaCommandCategory :: Stream.StreamCategory BetaCommand
betaCommandCategory = Stream.categoryUnsafe "beta"

type BetaEventStreamDef =
  EventStream (HsPred BetaRegs BetaCommand) BetaRegs BetaVertex BetaCommand BetaEvent

type BetaEventStream =
  ValidatedEventStream (HsPred BetaRegs BetaCommand) BetaRegs BetaVertex BetaCommand BetaEvent

betaEventStreamDef :: BetaEventStreamDef
betaEventStreamDef =
  EventStream
    { transducer = betaTransducer,
      initialState = BetaActive,
      initialRegisters = initialBetaRegs,
      eventCodec = betaCodec,
      resolveStreamName = Stream.streamName,
      snapshotPolicy = Never,
      stateCodec = Nothing
    }

betaEventStream :: BetaEventStream
betaEventStream =
  mkEventStreamOrThrow "Beta" betaEventStreamDef
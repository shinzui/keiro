{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE QualifiedDo #-}
-- @generated by keiro-dsl 0.14.0.0 (language keiro-dsl 4) from aggregate Beta; do not edit.
module Proof.WorkspaceProof.Beta.Generated.Transducer
  ( betaTransducer
  , betaFoldFingerprint
  , BehaviorOwnership (..)
  , betaPredicateVerifications
  ) where

import Proof.WorkspaceProof.Beta.Generated.Domain
import Data.Text (Text)
import Keiki.Builder qualified as B
import Keiki.Core (HsPred, SymTransducer)
import Keiki.Core qualified as K
import Keiki.Symbolic qualified as S

betaTransducer
  :: SymTransducer
       (HsPred BetaRegs BetaCommand)
       BetaRegs
       BetaVertex
       BetaCommand
       BetaEvent
betaTransducer =
  B.buildTransducer BetaActive initialBetaRegs isTerminal do
    B.from BetaActive do
      B.onCmd inCtorPingBeta $ \d -> B.do
        B.emit wireBetaPinged (BetaPingedTermFields
          { proofId = d.proofId
          })
        B.goto BetaActive
 where
  isTerminal = \case

    _ -> False

betaFoldFingerprint :: Text
betaFoldFingerprint = "6c99882f1ce386dec1b8be750be07d60"

data BehaviorOwnership = GeneratedOwned | HoleOwned
  deriving stock (Eq, Show)

-- Every checked transition predicate is audited through Keiki's conservative
-- symbolic verifier. Opaque Hole terms remain explicitly unverified.
betaPredicateVerifications :: IO [(Text, BehaviorOwnership, S.PredicateVerification)]
betaPredicateVerifications = sequence
  [ verifyTransition "transition1ActivePingBeta" GeneratedOwned BetaActive 0
  ]
 where
  verifyTransition label owner source edgeIndex =
    case drop edgeIndex (K.edgesOut betaTransducer source) of
      K.Edge predicate _ _ _ _ : _ -> (\result -> (label, owner, result)) <$> S.verifyPredicate predicate
      [] -> pure (label, owner, S.UnverifiedSolverFailure "generated transition edge missing")
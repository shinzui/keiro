{-# LANGUAGE OverloadedRecordDot #-}
-- @generated by keiro-dsl 0.11.0.0 (language keiro-dsl 5) from aggregate Hospital; do not edit.
module Generated.TransferRouting.Hospital.Codec (
    hospitalCodec,
    parseHospitalEvent,
    encodeHospitalEvent,
) where

import Generated.TransferRouting.Hospital.Domain
import Data.Aeson (Value, object, withObject, (.:), (.=))
import Data.Aeson.Types (parseEither)
import Data.List.NonEmpty (NonEmpty (..))
import Data.List.NonEmpty qualified as NonEmpty
import Data.Text (Text)
import qualified Data.Text as T
import Keiro.Codec (Codec (..), EventType (..))





hospitalEventTypes :: NonEmpty EventType
hospitalEventTypes = EventType "TransferNeedRouted" :| []

hospitalCodec :: Codec HospitalEvent
hospitalCodec =
  Codec
    { eventTypes = hospitalEventTypes
    , eventType = \case
        TransferNeedRouted{} -> EventType "TransferNeedRouted"
    , schemaVersion = 1
    , encode = encodeHospitalEvent
    , decode = parseHospitalEvent
    , upcasters = []
    }

encodeHospitalEvent :: HospitalEvent -> Value
encodeHospitalEvent = \case
  TransferNeedRouted payload ->
    object
      [ "kind" .= ("TransferNeedRouted" :: Text)
      , "transferNeedId" .= payload.transferNeedId
      , "hospitalId" .= payload.hospitalId
      ]

parseHospitalEvent :: EventType -> Value -> Either Text HospitalEvent
parseHospitalEvent (EventType tag) = mapLeftText . parseEither (withObject "HospitalEvent" go)
  where
    go o = do
      case tag of
        "TransferNeedRouted" ->
          TransferNeedRouted
            <$> ( TransferNeedRoutedData
                    <$> o .: "transferNeedId"
                    <*> o .: "hospitalId"
                )
        _ -> fail ("unknown event type " <> show tag <> "; expected one of: " <> renderExpectedEventTypes hospitalEventTypes)

mapLeftText :: Either String b -> Either Text b
mapLeftText = either (Left . T.pack) Right

renderExpectedEventTypes :: NonEmpty EventType -> String
renderExpectedEventTypes =
  T.unpack
    . T.intercalate ", "
    . map (\(EventType eventTypeName) -> eventTypeName)
    . NonEmpty.toList
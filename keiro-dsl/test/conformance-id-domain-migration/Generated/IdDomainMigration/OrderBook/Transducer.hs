{-# LANGUAGE BlockArguments #-}
{-# LANGUAGE DataKinds #-}
{-# LANGUAGE GADTs #-}
{-# LANGUAGE OverloadedRecordDot #-}
{-# LANGUAGE QualifiedDo #-}
{-# LANGUAGE TypeApplications #-}
-- @generated by keiro-dsl; do not edit. Regenerated from the .keiro spec.
module Generated.IdDomainMigration.OrderBook.Transducer
  ( orderBookTransducer
  , orderBookFoldFingerprint
  , BehaviorOwnership (..)
  , orderBookPredicateVerifications
  ) where

import Generated.IdDomainMigration.OrderBook.Domain
import Data.Text (Text)
import Keiki.Builder qualified as B
import Keiki.Core (HsPred, SymTransducer)
import Keiki.Core qualified as K
import Keiki.Symbolic qualified as S
import Generated.IdDomainMigration.OrderBook.Expressions qualified as Expressions
import Keiki.Builder ((=:))

orderBookTransducer
  :: SymTransducer
       (HsPred OrderBookRegs OrderBookCommand)
       OrderBookRegs
       OrderBookVertex
       OrderBookCommand
       OrderBookEvent
orderBookTransducer =
  B.buildTransducer OrderBookEmpty initialOrderBookRegs isTerminal do
    B.from OrderBookEmpty do
      B.onCmd inCtorRecord $ \d -> B.do
        B.slot @"orderId" =: Expressions.transition1EmptyRecordWriteOrderId d
        B.emit wireOrderRecorded (OrderRecordedTermFields
          { orderId = d.orderId
          })
        B.goto OrderBookRecorded
 where
  isTerminal = \case
    OrderBookRecorded -> True
    _ -> False

orderBookFoldFingerprint :: Text
orderBookFoldFingerprint = "ce837676f16ed1a5"

data BehaviorOwnership = GeneratedOwned | HoleOwned
  deriving stock (Eq, Show)

-- Every checked transition predicate is audited through Keiki's conservative
-- symbolic verifier. Opaque Hole terms remain explicitly unverified.
orderBookPredicateVerifications :: IO [(Text, BehaviorOwnership, S.PredicateVerification)]
orderBookPredicateVerifications = sequence
  [ verifyTransition "transition1EmptyRecord" GeneratedOwned OrderBookEmpty 0
  ]
 where
  verifyTransition label owner source edgeIndex =
    case drop edgeIndex (K.edgesOut orderBookTransducer source) of
      K.Edge predicate _ _ _ _ : _ -> (\result -> (label, owner, result)) <$> S.verifyPredicate predicate
      [] -> pure (label, owner, S.UnverifiedSolverFailure "generated transition edge missing")
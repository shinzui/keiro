-- @generated by keiro-dsl 0.13.0.0 (language keiro-dsl 4) from context nominal-scalars replay-audit assembly; do not edit.
--
-- Deployment contract:
--   * replay-neutral diff: no data audit is required;
--   * affected diff: run AuditTargeted with the emitted affected set
--     against a production copy under the candidate binary;
--   * one-time runtime cutover: run AuditFull;
--   * any non-zero audit exit blocks deployment.
module Generated.NominalScalars.ReplayAudit (auditTargets) where

import Generated.NominalScalars.NominalLedger.EventStream qualified as NominalLedger
import Keiro.ReplayAudit (AuditTarget (..), SomeAuditTarget (..), streamInCategory)
import Keiro.Stream qualified as Stream

auditTargets :: [SomeAuditTarget]
auditTargets =
  [ SomeAuditTarget
      AuditTarget
        { eventStream = NominalLedger.nominalLedgerEventStream
        , category = Stream.categoryText NominalLedger.nominalLedgerCategory
        , mkStream = streamInCategory (Stream.categoryText NominalLedger.nominalLedgerCategory)
        }
  ]
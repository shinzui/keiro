-- @generated by keiro-dsl 0.13.0.0 (language keiro-dsl 5) from aggregate Orders; do not edit.
module Generated.CatalogDemo.Orders.EventStream
  ( ordersCategory
  , ordersCommandCategory
  , ordersEventStream
  , ordersEventStreamDef
  , OrdersEventStream
  , OrdersEventStreamDef
  ) where

import Generated.CatalogDemo.Orders.Domain
import Generated.CatalogDemo.Orders.Codec (ordersCodec)
import Generated.CatalogDemo.Orders.Transducer (ordersTransducer)
import Keiki.Core (HsPred)
import Keiro.EventStream (EventStream (..), SnapshotPolicy (..))
import Keiro.EventStream.Validate (ValidatedEventStream, mkEventStreamOrThrow)
import Keiro.Stream qualified as Stream

-- The validated aggregate stream category (hole-kind 5: referenced, never retyped).
-- Entity streams are '<category>-<id>' via Keiro.Stream.entityStream.
-- categoryUnsafe is safe here because this generated literal passed the DSL category proof.
ordersCategory :: Stream.StreamCategory OrdersEventStreamDef
ordersCategory = Stream.categoryUnsafe "orders"

-- The same category text, typed for command envelopes such as PMCommand.
ordersCommandCategory :: Stream.StreamCategory OrdersCommand
ordersCommandCategory = Stream.categoryUnsafe "orders"

type OrdersEventStreamDef =
  EventStream (HsPred OrdersRegs OrdersCommand) OrdersRegs OrdersVertex OrdersCommand OrdersEvent

type OrdersEventStream =
  ValidatedEventStream (HsPred OrdersRegs OrdersCommand) OrdersRegs OrdersVertex OrdersCommand OrdersEvent

ordersEventStreamDef :: OrdersEventStreamDef
ordersEventStreamDef =
  EventStream
    { transducer = ordersTransducer,
      initialState = OrdersEmpty,
      initialRegisters = initialOrdersRegs,
      eventCodec = ordersCodec,
      resolveStreamName = Stream.streamName,
      snapshotPolicy = Never,
      stateCodec = Nothing
    }

ordersEventStream :: OrdersEventStream
ordersEventStream =
  mkEventStreamOrThrow "Orders" ordersEventStreamDef
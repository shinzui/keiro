context beta

id BetaId prefix=bet

aggregate Beta
  regs
    betaId BetaId = placeholder
    state BetaVertex = Pending
  states Pending Done!

  command DoThing { betaId attempt:Int }
  event ThingCompleted { betaId attempt:Int }

  Pending -- DoThing -->
    write state := Done
    emit ThingCompleted
    goto Done

  wire kind=ctorName fields=camelCase schemaVersion=1

{-# LANGUAGE GADTs #-}
-- @generated by keiro-dsl; do not edit. Regenerated from the .keiro spec.
--
-- Deployment contract:
--   * replay-neutral diff: no data audit is required;
--   * affected diff: run AuditTargeted with the emitted affected set
--     against a production copy under the candidate binary;
--   * one-time runtime cutover: run AuditFull;
--   * any non-zero audit exit blocks deployment.
module Generated.WorkspaceNominalProof.ReplayAudit (auditTargets) where

import Generated.WorkspaceNominalProof.ProjectArtifact.EventStream qualified as ProjectArtifact
import Generated.WorkspaceNominalProof.Project.EventStream qualified as Project
import Keiro.ReplayAudit (AuditTarget (..), SomeAuditTarget (..), streamInCategory)
import Keiro.Stream qualified as Stream

auditTargets :: [SomeAuditTarget]
auditTargets =
  [ SomeAuditTarget
      AuditTarget
        { eventStream = ProjectArtifact.projectArtifactEventStream
        , category = Stream.categoryText ProjectArtifact.projectArtifactCategory
        , mkStream = streamInCategory (Stream.categoryText ProjectArtifact.projectArtifactCategory)
        }
  , SomeAuditTarget
      AuditTarget
        { eventStream = Project.projectEventStream
        , category = Stream.categoryText Project.projectCategory
        , mkStream = streamInCategory (Stream.categoryText Project.projectCategory)
        }
  ]
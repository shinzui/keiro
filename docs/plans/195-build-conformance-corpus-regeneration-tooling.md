---
id: 195
slug: build-conformance-corpus-regeneration-tooling
title: "Build conformance corpus regeneration tooling"
kind: exec-plan
created_at: 2026-08-05T04:54:27Z
intention: "intention_01kz84b5jre3187dmmyjmd02fc"
master_plan: "docs/masterplans/29-stabilize-keiro-dsl-for-wide-adoption.md"
---

# Build conformance corpus regeneration tooling

This ExecPlan is a living document. The sections Progress, Surprises & Discoveries,
Decision Log, and Outcomes & Retrospective must be kept up to date as work proceeds.
If durable project context changes, update or create ADRs in docs/adr/ in the same change.


## Purpose / Big Picture

After this work, an intentional generator-output change becomes a one-command corpus
refresh reviewed as an ordinary diff. Today the repository commits the complete output of
`keiro-dsl scaffold` for dozens of fixture services — the "conformance corpus" — and
compiles it in 35 cabal test suites, so any change to generated bytes requires a human to
re-run every historical scaffold command by hand, re-commit every tree, and reconcile the
cabal module inventories. History shows the cost: whole-corpus migration commits such as
`cd22e7f2` ("test(dsl): migrate the stable fixture corpus") and `86b2dfa6` ("feat(dsl):
migrate generated naming corpus", 113 files changed) accompany every output-affecting
plan. That churn tax fossilizes known output defects because improving them is expensive;
MasterPlan 29's Phase 3 polish plan (EP-196) hard-depends on removing it.

This plan delivers a regeneration driver (`keiro-dsl-corpus-regen`) that replays every
committed scaffold through the public `keiro-dsl` CLI with the exact flags each tree was
created with, overwrites only banner-recognized generated files, preserves create-once
hand-owned modules exactly as the scaffolder does, updates the plain `.golden` fixture
files through the tests that own them, verifies the cabal module inventories against the
regenerated trees, and finishes with a git diff summary for human review. It never
commits. Its own acceptance is conservative and observable: run on today's clean tree,
regeneration changes zero bytes (`git status --porcelain` is empty); deliberately corrupt
one generated file and the tool restores it byte-identically; put a sentinel edit into a
create-once Hole module and it survives untouched. A repository policy script wires the
zero-diff property into `just verify` so corpus drift can never land silently.


## Progress

- [x] (2026-08-04) Research the complete corpus: enumerate all 34 committed scaffold
  histories (32 single-spec records, 2 workspace records), the 41 public-CLI invocations
  that reproduce them, the 35 cabal test suites (and 4 further components) that compile
  them, the 6 `.golden` fixtures, and the record/banner provenance model; author this
  plan under Intention `intention_01kz84b5jre3187dmmyjmd02fc`.
- [x] (2026-08-05 10:02 PDT) Reconcile the plan with the working tree: the generated
  histories are committed, but 33 of their 34 authoritative record/manifest pairs were
  ignored and untracked. Decide to force-add those corpus sidecars while retaining the
  repository-wide ignore rule for ordinary consumer scaffolds; exclude the stale nested
  `conformance-skeletons/SkelAggregate` record/manifest pair.
- [x] (2026-08-05 10:10 PDT) Milestone 1: add the tracked corpus provenance, supplement
  manifest, and `keiro-dsl-corpus-regen` package; regenerate
  `test/conformance-behavior-complete` across 1 of 41 invocations with zero generated-byte
  diff, restore a removed import, preserve a create-once sentinel, and prove bannerless
  overwrite refusal. `cabal build keiro-dsl-corpus-regen` and the extension policy pass.
- [ ] Milestone 2: full-corpus coverage (single, workspace, skeleton, and
  codec-comparison invocations), record/disk/cabal consistency checks, and the
  `.golden` update mode; whole-corpus regeneration on a clean tree produces zero diff.
- [ ] Milestone 3: `check` policy mode, `scripts/check-conformance-corpus.sh`, Justfile
  and `verify` wiring, contributor documentation, changelogs, and closure.


## Surprises & Discoveries

- Observation: Scaffold records already carry most of the provenance a regeneration
  manifest needs. Every single-spec record stores the spec path relativized against
  `git rev-parse --show-toplevel` (so it is repository-relative and cwd-independent),
  plus `module-root:` and `layout:` lines that determine the `--module-root` and
  `--collocate` flags.
  Evidence: `keiro-dsl/app/Main.hs` lines 317–323 (`makeRelative repoRoot absFp`) and
  the `spec:` header in every `keiro-dsl-scaffold-record.*.txt`.

- Observation: Three histories are not reproducible from records alone. The
  `test/conformance-skeletons` record says `spec: /dev/stdin` (the tree is eight
  successive stdin scaffolds of `keiro-dsl new <kind>` output, and only the last run's
  record survives because all eight share context `my-service`); workspace records store
  only the manifest basename (`manifest: service.keiro-workspace`), not its repository
  location; and the structural tree's codec-comparison module is emitted by opt-in flags
  that are deliberately not recorded ("opt-in tooling output and is not part of the
  production scaffold record", `keiro-dsl/keiro-dsl.cabal` comment above
  `keiro-dsl-conformance-codec-compare`). This forces the hybrid manifest design in the
  Decision Log.

- Observation: The committed corpus embeds no hand-captured golden payloads. The files
  under `keiro-dsl/test/golden-payloads/` are consumed only by in-test scaffolds in
  `keiro-dsl/test/Main.hs`; no committed tree contains their bytes (verified by grepping
  the distinctive `rsv_01hzy3v7q2e8kaw2m5x0d41n9c` value), and no `golden-payloads`
  directory exists beside any fixture spec, so the CLI's default golden root resolves to
  nothing for every committed tree. Regeneration therefore needs no `--goldens` flag.

- Observation: The cabal inventories deliberately compile subsets, so "generate the
  stanzas" is unsound. `keiro-dsl-conformance-codec-compare` compiles 10 hand-picked
  modules across two trees, the `keiro-dsl-codec-bench` benchmark compiles another
  subset of the structural tree, and
  `Generated.StructuralConformance.ArtifactCatalog.BehaviorContract` is committed and
  recorded but compiled by no component at all today. See the Decision Log entry
  choosing a checker with exemptions over stanza generation.

- Observation: The plan's record-derived discovery could not work in a clean checkout as
  drafted. `.gitignore` ignores every `keiro-dsl-scaffold-record.*.txt` and
  `keiro-dsl-manifest.*.txt`; only the newsurface pair was already force-tracked. The local
  tree also held one stale nested skeleton pair under
  `keiro-dsl/test/conformance-skeletons/SkelAggregate/`, while the authoritative eight-run
  skeleton history is the root pair whose last writer is `SkelWorkflow`.
  Evidence: `git ls-files keiro-dsl/test | rg 'keiro-dsl-scaffold-record'` returned one
  path, while the filesystem held 35 records; excluding the nested stale pair leaves the
  intended 34 histories.

- Observation: Single-spec and workspace scaffold reports have deliberately different
  idempotence vocabulary. The single-spec `writeModule` always writes a recognized
  Generated target and reports `(overwritten)`; only the workspace path compares bytes
  before writing and can report `(unchanged)`. Git still reports zero byte diff after the
  representative single-spec regeneration.
  Evidence: `WriteDisposition`'s comment and `writeModule` in
  `keiro-dsl/src/Keiro/Dsl/ScaffoldRun.hs` state this explicitly; the Milestone 1 run
  reported 12 overwritten Generated modules and left every tracked generated module
  byte-identical.


## Decision Log

- Decision: Derive the corpus inventory from the committed scaffold records, with one
  small checked-in supplement file (`keiro-dsl/test/conformance-corpus-manifest.txt`)
  for what the trees cannot state: the eight ordered skeleton invocations, the two
  workspace manifest locations, per-tree extra flags (the structural codec-comparison
  pair), and the uncompiled-generated-module exemption list.
  Rationale: Records already store repository-relative spec paths, module roots, and
  layout, and they are regenerated by every run, so deriving from them removes double
  bookkeeping — a new conformance tree joins the corpus the moment its record is
  committed. Hand-maintaining only the three unrecoverable facts keeps the supplement
  a dozen lines instead of a forty-entry mirror that would drift.
  Date: 2026-08-04

- Decision: Force-track the 34 authoritative scaffold-record and build-manifest pairs as
  corpus evidence, while preserving `.gitignore` for scaffold metadata outside the test
  corpus. Do not track the stale nested `conformance-skeletons/SkelAggregate` pair.
  Rationale: `git ls-files` is deliberately the driver's discovery boundary, so every
  provenance record it needs must exist in a clean checkout. Removing the global ignore
  rule would make consumer-local scaffold metadata appear as ordinary untracked files;
  force-tracking the bounded corpus evidence preserves both behaviors. The root skeleton
  record is authoritative because the declared eight invocations share one output and
  `SkelWorkflow` must be the final writer.
  Date: 2026-08-05

- Decision: Define corpus idempotence by resulting tracked bytes, while retaining the
  public CLI's existing per-path dispositions as supporting evidence rather than requiring
  `(unchanged)` from single-spec runs.
  Rationale: The driver must replay the public CLI without forking its write semantics.
  Workspace runs prove no-write idempotence through `(unchanged)`; single-spec runs prove
  byte idempotence through an empty Git diff after their expected `(overwritten)` report.
  Changing single-spec write behavior is outside this tooling plan.
  Date: 2026-08-05

- Decision: Implement the driver as a Haskell executable in a new local package
  `keiro-dsl-corpus-regen` under `keiro-dsl/tools/corpus-regen/`, not as a shell script
  and not as a second executable in the `keiro-dsl` package; thin Justfile recipes and
  one policy shell script wrap it.
  Rationale: The driver must parse scaffold and workspace records — and the real
  parsers already exist as exposed library modules (`Keiro.Dsl.ScaffoldRecord`,
  `Keiro.Dsl.WorkspaceRecord`), so a Haskell driver can never drift from the record
  grammar the way a shell re-parser would. The cabal-inventory check needs genuine
  `.cabal` parsing (`Cabal-syntax`), which bash cannot do precisely. The repository
  convention keeps the `keiro-dsl` package at exactly one executable and uses a
  separate local package for extra binaries (stated in
  `keiro-dsl/test/keiro-dsl-codec-compare-artifact-info.cabal`), so a new local
  package is the sanctioned home. Shell remains for the CI entry point,
  matching `scripts/check-extension-policy.sh` conventions.
  Date: 2026-08-04

- Decision: The cabal-inventory deliverable is a consistency checker that fails with a
  precise per-component diff, not generation of the `other-modules` stanzas; generated
  modules compiled by no component must appear in an explicit exemption list in the
  supplement manifest to pass.
  Rationale: Generation was preferred in principle but is infeasible here: corpus
  components deliberately compile subsets (the codec-compare suite and the benchmark
  pick modules out of the structural tree), the stanzas interleave with hand-written
  comments and per-suite `build-depends`, and one recorded generated module
  (`ArtifactCatalog/BehaviorContract.hs` in the structural tree) is compiled nowhere
  today. A generator would either flatten those deliberate choices or have to model
  them, gaining nothing over a checker whose failure output is the exact edit to make.
  The exemption list makes today's known gap visible and reviewable instead of silent,
  and hands it to EP-196 as an explicit finding.
  Date: 2026-08-04

- Decision: `.golden` updates run through the tests that own them: a
  `KEIRO_DSL_UPDATE_GOLDENS=1` accept mode added to a shared helper in
  `keiro-dsl/test/Main.hs`, invoked by the driver's `update-goldens` subcommand.
  Rationale: The six golden comparisons are produced by library renderers
  (`renderFoldBaseline`, diff finding/explain renderers, the diff-report JSON) inside
  `keiro-dsl/test/Main.hs`; no public CLI invocation reproduces those exact texts. Any
  external re-implementation would drift from what the assertions compare. Writing the
  accepted bytes from inside the same assertion site guarantees the updated golden is
  exactly what the test verifies.
  Date: 2026-08-04

- Decision: `check` mode refuses to start when corpus paths are already dirty, then
  regenerates in place and fails on any resulting diff; it does not regenerate into a
  temporary copy.
  Rationale: Records relativize spec paths against the out-directory's git root, so
  scaffolding into a copy outside the repository would change record bytes and
  manufacture false diffs. Refusing a dirty corpus keeps tool output from mixing with
  uncommitted human work, and on failure the drift left in the working tree is exactly
  the diff a reviewer needs — the tool never commits, so nothing is lost.
  Date: 2026-08-04

- Decision: Skeleton invocations execute in the fixed order of
  `skeletonModuleRoots` in `keiro-dsl/test/Main.hs` (aggregate, process, router,
  contract, intake, emit, workqueue, workflow), and the supplement manifest preserves
  that order.
  Rationale: All eight skeleton scaffolds share context `my-service` in one directory,
  so each run overwrites the shared record and only the last survives. The committed
  record says `module-root: SkelWorkflow`; byte-identical regeneration therefore
  requires workflow to run last. Order is part of the corpus contract and must be
  declarative, not incidental.
  Date: 2026-08-04

- Decision: Amend ADR 0015 with the clean-checkout conformance-history rule discovered
  during implementation; otherwise this plan enforces existing decisions rather than
  selecting a new product architecture.
  Rationale: The create-once/overwriteable and banner-provenance boundaries were already
  durable, but record-derived regeneration adds one repository-level consequence: every
  committed conformance history must force-track its authoritative record/manifest twins
  even though ordinary consumer metadata remains ignored. Without that rule, the same
  checkout can regenerate while CI cannot.
  Date: 2026-08-05 (revises the 2026-08-04 no-ADR expectation after implementation evidence)

- Decision: Out of scope, recorded as future work: changing any generated output byte
  (zero-diff on the current corpus is this plan's acceptance), reducing the 418
  `T.isInfixOf` substring assertions in `keiro-dsl/test/Main.hs`, and hardening the sed
  patterns in the twelve `keiro-dsl/test/*.sh` mutation scripts.
  Rationale: EP-196 owns output changes and will spend that churn once, through this
  tool; assertion and mutation-script robustness are independent maintainability items
  that would dilute this plan's single observable guarantee.
  Date: 2026-08-04


## Outcomes & Retrospective

Implementation has not started. At completion, record the final invocation count, the
exact exemption list the checker required, the measured wall-clock cost of a full
regeneration and of the `check` gate, any tree that failed to reproduce byte-identically
on the first full run (each such case is a real finding for the Surprises section), and
whether EP-196 can begin. Compare against the purpose: one command, zero diff on a clean
tree, ordinary reviewable diff after an intentional generator change.


## Context and Orientation

### What the conformance corpus is

`keiro-dsl` (package directory `keiro-dsl/`, library under `keiro-dsl/src/`, single CLI
executable built from `keiro-dsl/app/Main.hs`) compiles `.keiro` service specifications
into Haskell modules. The repository proves the generator against real code by
committing complete scaffold outputs — generated modules, hand-filled "Hole" modules,
scaffold records, and build manifests — and compiling them in dedicated cabal test
suites. This committed material is the *conformance corpus*.

Two file classes matter (defined by
[ADR 0015](../adr/0015-workspace-scaffold-history-is-workspace-keyed-with-attributable-adoption.md)
and
[ADR 0019](../adr/0019-generated-haskell-has-an-explicit-edition-and-local-extension-contract.md)):

- *Overwriteable generated files* carry a provenance banner as an exact line, currently
  shaped `-- @generated by keiro-dsl <package-version> (language keiro-dsl <effective
  version>) from <stable-node-origin>; do not edit.` (a historical fixed line is also
  recognized; see `isGeneratedBannerLine` in `keiro-dsl/src/Keiro/Dsl/Scaffold.hs`,
  around line 6249). The single-spec path overwrites these deterministically; the
  workspace path compares bytes first and reports `(unchanged)` without writing when they
  match. Both paths *refuse the whole run*
  (`MissingGeneratedBanner`, `keiro-dsl/src/Keiro/Dsl/ScaffoldRun.hs` around line 540)
  if a planned generated path exists without the banner, unless
  `--force-generated-overwrite` is passed.
- *Create-once files* (Hole modules such as `BehaviorHoles.hs`, application `Holes.hs`,
  bindings skeletons, conformance `Expectations.hs`, and the hand-owned runtime cabal
  file) are written only when absent; later runs report `(skipped: already present)`
  and never rewrite them. They are hand-owned application code.

A third banner exists: the opt-in codec-comparison module begins
`-- @generated by keiro-dsl codec comparison; non-production migration evidence; do not
edit.` and has its own banner-guarded overwrite preflight (`preflightComparison`,
`keiro-dsl/app/Main.hs` around line 739).

Every scaffold also writes a *scaffold record* — `keiro-dsl-scaffold-record.<context>.txt`
for single-spec runs, `keiro-dsl-scaffold-record.workspace.<service>.txt` for workspace
runs — listing the spec path (repository-relative), `module-root:`, `layout:`
(`prefixed` or `collocated`), and every `generated`/`hole` path, plus a build-manifest
twin (`keiro-dsl-manifest.*.txt`). Records are themselves committed corpus bytes and
must reproduce exactly.

### The exact inventory (verified 2026-08-04)

There are 35 `test/conformance*` directories plus the service-package workspace. They
hold 34 committed scaffold histories:

- 31 ordinary single-spec histories, one per directory, each fully described by its
  record. Examples: `keiro-dsl/test/conformance/` from
  `keiro-dsl/test/fixtures/reservation.keiro`; `keiro-dsl/test/conformance-behavior-complete/`
  from `keiro-dsl/test/fixtures/behavior-complete.keiro`;
  `keiro-dsl/test/conformance-router*/` (three directories) from
  `keiro-dsl/test/fixtures/incident-paging/incident-paging.keiro`. All 31 have
  `module-root: (none)` and `layout: prefixed`, so their invocation is plain
  `scaffold <spec> --out <dir>`. The structural history
  (`keiro-dsl/test/conformance-structural/`, from
  `keiro-dsl/test/fixtures/structural-conformance.keiro`) additionally owns the
  committed comparison module
  `Generated/StructuralConformance/Structural/CodecCompare/ArtifactInfo.hs`, produced by
  adding `--codec-comparison ArtifactInfo --comparison-out <that path>` to the same
  invocation.
- 1 skeleton history: `keiro-dsl/test/conformance-skeletons/` holds eight generated
  trees under module roots `SkelAggregate`, `SkelProcess`, `SkelRouter`, `SkelContract`,
  `SkelIntake`, `SkelEmit`, `SkelQueue`, `SkelWorkflow`, each produced by piping
  `keiro-dsl new <kind>` into `keiro-dsl scaffold /dev/stdin --out <dir>
  --module-root <Root>` (the list and roots are `skeletonModuleRoots` in
  `keiro-dsl/test/Main.hs`, line 7202). Its surviving record shows `spec: /dev/stdin`
  and `module-root: SkelWorkflow` (last writer).
- 2 workspace histories: `keiro-dsl/test/conformance-workspace-nominals/` from
  `keiro-dsl/test/fixtures/workspace-nominals/service.keiro-workspace`, and the
  runnable service-package proof scaffolded from
  `keiro-dsl/test/conformance-service-package/service.keiro-workspace` into
  `keiro-dsl/test/conformance-service-package/runtime/src/`. Both manifests declare
  their own module-root/layout/runtime-package authority, so the invocation is plain
  `scaffold <manifest> --out <dir>` — the existing public-CLI idempotence test
  ("scaffolds the multi-member proof idempotently through the public CLI",
  `keiro-dsl/test/Main.hs` line 4999) already proves this for the service package. The
  workspace run also regenerates a whole conformance cabal package
  (`.../keiro-dsl-conformance.workspace.workspace-proof/`, including its `.cabal` file
  and `keiro-dsl-conformance-record.txt`) while skipping the create-once
  `src/KeiroConformance/Expectations.hs`.

Total: 41 public-CLI scaffold invocations (31 single + 8 skeleton + 2 workspace)
reproduce the whole corpus. `keiro-dsl/test/conformance-codec-compare/` holds no
scaffold history of its own — only hand-written consumer code and JSON fixture corpora;
its generated import lives in the structural tree.

The corpus is compiled by 35 cabal test suites: 34 `keiro-dsl-conformance*` suites in
`keiro-dsl/keiro-dsl.cabal` (1117 lines; each suite's `other-modules` names the modules
it compiles) plus the generated `conformance` suite of package
`keiro-workspace-proof-conformance` listed in the repository `cabal.project`. Four more
components compile corpus trees: the `keiro-dsl-codec-bench` benchmark, the
`keiro-dsl-behavior-complete-report` and `keiro-dsl-codec-compare-artifact-info`
executables, and the hand-owned `keiro-dsl-conformance-service-runtime` library. The
MasterPlan's "~38 suites" estimate resolves to exactly 35 test suites, 39 components.

Separately, six plain-text *golden files* pin renderer output under
`keiro-dsl/test/fixtures/`: `fold-identity-baseline.golden`,
`fold-identity-diff-replay.golden`, `compatibility-vector.diff.golden`,
`contract-typeid-domain.diff.golden`, `contract-typeid-domain.diff.json.golden`, and
`workspace-diff-new/workspace.diff.golden`. Their tests (in `keiro-dsl/test/Main.hs` at
lines 195, 308, 3803, 4072–4073, and 5987) read the file with `readTestText` and compare
with `shouldBe` after `T.stripEnd`; today an intentional change fails with a wall of
expected/actual text and the contributor pastes new bytes by hand.

### Why regeneration is currently expensive

The repository has no accept mode, regeneration script, or fixture-refresh tool of any
kind: nothing outside the twelve mutation shell scripts in `keiro-dsl/test/*.sh` (which
mutate copies to prove tests catch defects, and never refresh committed trees), and no
accept or regen hook in `keiro-dsl/test/Main.hs` or `keiro-dsl/app/Main.hs`. An
intentional generator-output change therefore means: re-run 41 historical scaffold
invocations by hand with the right flags, review and re-commit every tree, adjust the
`other-modules` inventories in the 1117-line `keiro-dsl/keiro-dsl.cabal` when layout
shifts, revisit as needed the 418 `T.isInfixOf` substring assertions in the 9215-line
`keiro-dsl/test/Main.hs`, and hand-edit six golden files. The corpus discipline itself
is good — banners, refusal of bannerless overwrites, create-once preservation, semantic
compiled suites, and normalized byte comparisons (`normalizeGenerated` in
`keiro-dsl/test/Main.hs` compares imports and pragmas as sets) all already exist. What
is missing is exactly one driver that replays the recorded provenance.

### Relevant ADRs

The local ADR scan found four relevant accepted decisions; no ADR covers regeneration
tooling itself (none is expected to — see the Decision Log):

- [ADR 0015](../adr/0015-workspace-scaffold-history-is-workspace-keyed-with-attributable-adoption.md)
  — the create-once versus overwriteable boundary, the two recognized banner forms,
  workspace-keyed records, observable idempotence (`(unchanged)` reporting), and the
  rule that stale evidence never authorizes deletion. The tool must inherit all of this
  by driving the public CLI rather than re-implementing writes.
- [ADR 0019](../adr/0019-generated-haskell-has-an-explicit-edition-and-local-extension-contract.md)
  — the generated compilation contract and the rule that only overwriteable `Generated`
  modules are ever regenerated; create-once modules stay hand-owned.
- [ADR 0004](../adr/0004-evolution-changes-are-gated-at-the-earliest-sound-boundary.md)
  — failures surface at the earliest boundary with enough evidence; the corpus policy
  script applies the same stance to corpus drift (refused at `verify`/CI, not at
  release).
- [ADR 0014](../adr/0014-service-workspaces-compose-single-owner-members-under-one-manifest-identity.md)
  — the workspace manifest is the authority for service name, module root, layout, and
  runtime package, which is why workspace regeneration needs no CLI flags.

Cross-repository context: none required. The tool touches no Keiki interface directly;
the corpus suites' dependency on `mori://shinzui/keiki/packages/keiki` is unchanged.


## Plan of Work

### Milestone 1: Manifest, driver package, and one suite regenerated with zero diff

This milestone creates the supplement manifest and the driver package, and proves the
core loop on a single representative history (`test/conformance-behavior-complete`,
which exercises generated modules, create-once Holes and BehaviorHoles, a record, and a
build manifest). At the end, one command regenerates that tree with zero diff, restores
a corrupted generated file, and preserves a create-once sentinel.

Create `keiro-dsl/test/conformance-corpus-manifest.txt` with the version header
`keiro-dsl conformance corpus manifest v1` followed by line-oriented rows. Rows use
repository-relative paths. Three row kinds exist in Milestone 1 (the fourth,
`uncompiled-generated`, arrives in Milestone 2):

```text
workspace <manifest-path> <out-dir>
skeleton <kind> <module-root> <out-dir>
extra-args <out-dir> <arg>...
```

Seed it with the two workspace rows, the eight skeleton rows in `skeletonModuleRoots`
order (workflow last), and one `extra-args` row for `keiro-dsl/test/conformance-structural`
carrying `--codec-comparison ArtifactInfo --comparison-out
keiro-dsl/test/conformance-structural/Generated/StructuralConformance/Structural/CodecCompare/ArtifactInfo.hs`.
Begin the file with `#`-prefixed comment lines documenting the format, since the
manifest is also contributor documentation.

Before the driver performs record discovery, force-add the 34 authoritative
`keiro-dsl-scaffold-record.*.txt` files and their 34 `keiro-dsl-manifest.*.txt` twins
under the corpus output roots. Keep the repository-wide ignore rules unchanged, and do
not add the stale nested pair under `keiro-dsl/test/conformance-skeletons/SkelAggregate/`.
This turns the already-generated provenance into clean-checkout input without changing
any generated bytes.

Create the package `keiro-dsl/tools/corpus-regen/` containing
`keiro-dsl-corpus-regen.cabal` (one executable, `keiro-dsl-corpus-regen`, `main-is:
Main.hs`, modules `Main` and `CorpusPlan`) and add the directory to the `packages`
stanza of the repository `cabal.project`. The package depends on the `keiro-dsl`
library so `CorpusPlan` parses records with the real exposed parsers
(`Keiro.Dsl.ScaffoldRecord`, `Keiro.Dsl.WorkspaceRecord`); see Interfaces and
Dependencies for the full dependency list and internal signatures. The cabal file
declares the shared `default-extensions` (`ImportQualifiedPost`, `OverloadedLabels`)
that `scripts/check-extension-policy.sh` requires of every repository-authored cabal
file — run that script before finishing the milestone to confirm.

`Main.hs` implements three subcommands (`regenerate`, `check`, `update-goldens`; the
latter two are completed in Milestones 2–3) plus `--help` text describing the corpus,
the manifest format, and the review workflow. `regenerate` performs:

1. Resolve the repository root with `git rev-parse --show-toplevel` and treat all paths
   relative to it.
2. Load and validate the supplement manifest.
3. Discover every committed scaffold record with
   `git ls-files 'keiro-dsl/test/**/keiro-dsl-scaffold-record*.txt'` and parse each.
   Derive one invocation per single-spec record: `scaffold <recSpecPath> --out
   <record-directory>`, adding `--module-root <X>` when the record's module-root is not
   `(none)`, `--collocate` when its layout is `collocated`, and any `extra-args` row
   for that directory. A record whose spec is `/dev/stdin` must be claimed by skeleton
   rows for the same directory (error otherwise, and error if skeleton rows exist for a
   directory with no such record); a workspace record must be claimed by a `workspace`
   row whose out-dir matches (error otherwise, in both directions). This
   cross-validation is what keeps the supplement honest against the trees.
4. Resolve the `keiro-dsl` binary once via `cabal list-bin keiro-dsl` (after `cabal
   build keiro-dsl exe:keiro-dsl`) and execute invocations directly, in deterministic
   order: all single and workspace invocations sorted by out-dir, then the skeleton
   rows in manifest order. Skeleton invocations run `keiro-dsl new <kind>`, capture
   stdout, and feed it as the child's stdin to `keiro-dsl scaffold /dev/stdin --out
   <dir> --module-root <Root>`. Stream each invocation's stderr report (the
   `(overwritten)` / `(unchanged)` / `(skipped: already present)` lines) to the operator.
5. A non-zero exit from any invocation (for example a `MissingGeneratedBanner` refusal)
   stops the run with the CLI's own message; the tool adds only which invocation
   failed. It never passes `--force-generated-overwrite`.
6. Finish by printing `git status --porcelain --` and `git diff --stat --` over the
   corpus paths (every derived out-dir plus `keiro-dsl/test/fixtures`), with a closing
   line telling the operator to review and commit; the tool itself never invokes
   `git add` or `git commit`.

Support `--only <out-dir>` (repeatable) to restrict the invocation set, for fast
iteration and for this milestone's acceptance.

Acceptance for Milestone 1 (commands in Concrete Steps): on a clean tree,
`regenerate --only keiro-dsl/test/conformance-behavior-complete` exits zero, reports
every generated module `(overwritten)` on this single-spec path, and leaves no tracked
byte diff; workspace entries may instead report `(unchanged)`. After
deleting a line from a committed generated module (keeping its banner) the same command
restores it byte-identically; after deleting the file outright it is recreated
byte-identically; a sentinel comment appended to the create-once
`BehaviorComplete/Journey/BehaviorHoles.hs` survives regeneration and is then reverted
by hand; removing the banner line from a generated module makes the run fail with the
scaffolder's `MissingGeneratedBanner` refusal and no overwrite.

### Milestone 2: Full corpus coverage, consistency checks, and golden update mode

This milestone extends coverage to all 41 invocations and adds the three consistency
layers. At the end, a bare `regenerate` on a clean tree reproduces the entire corpus
with zero diff — the empirical proof that the manifest, flag derivation, skeleton
ordering, and comparison flags are all correct. Any tree that does not reproduce
byte-identically is a real discovery: diagnose it (wrong derived flags versus genuine
generator drift), record it in Surprises & Discoveries, and fix the manifest or record
the drift for its owning plan; do not paper over it by committing tool-produced
changes without understanding them.

Add the consistency checks, run after regeneration in both `regenerate` and `check`
modes:

- *Record/disk*: every `generated` and `hole` row in every record names an existing
  file; every `.hs` file under a corpus tree whose head carries a recognized generated
  banner appears as a `generated` row in that tree's record, except the
  codec-comparison module, which must instead match an `extra-args` row's
  `--comparison-out` path. Failures name the record and the path.
- *Cabal/disk*: parse `keiro-dsl/keiro-dsl.cabal` with `Cabal-syntax`
  (`Distribution.PackageDescription.Parsec.parseGenericPackageDescriptionMaybe`). For
  every component whose `hs-source-dirs` intersect corpus directories: each
  `other-modules` entry must resolve to a file in that component's source dirs
  (dangling entries fail, naming component and module). In the other direction, every
  generated module on disk in a corpus tree must be compiled by at least one such
  component, or be listed in an `uncompiled-generated <path>` exemption row in the
  supplement manifest. Do not check the generated
  `keiro-workspace-proof-conformance.cabal` — it is itself regenerated output. Seed
  the exemption list from the checker's first honest run; the already-verified entry is
  `keiro-dsl/test/conformance-structural/Generated/StructuralConformance/ArtifactCatalog/BehaviorContract.hs`.
  Review each seeded exemption deliberately and hand the list to EP-196 as findings.
- *Manifest/record cross-validation* from Milestone 1 (already implemented; now
  exercised over the full set).

Implement the golden update mode. In `keiro-dsl/test/Main.hs`, add one helper:

```haskell
assertMatchesGolden :: FilePath -> T.Text -> IO ()
```

It resolves the golden path with the existing `resolveTestPath` machinery, and when the
environment variable `KEIRO_DSL_UPDATE_GOLDENS` is set to `1` writes
`T.stripEnd actual <> "\n"` to the file and passes; otherwise it performs today's
comparison (`T.stripEnd actual shouldBe T.stripEnd golden`). Rewrite exactly the six
golden comparison sites (lines 195, 308, 3803, 4072, 4073, 5987) to use it, changing no
other assertion — the surrounding `T.isInfixOf` checks stay untouched and still guard
against accepting garbage. The driver's `update-goldens` subcommand runs `cabal test
keiro-dsl-test` with the variable set (passing through optional
`--test-options='--match ...'` for speed) and finishes with the same git summary as
`regenerate`. Because the substring assertions still run in accept mode, an update that
would produce structurally wrong output still fails instead of being accepted.

Acceptance for Milestone 2: full `regenerate` on a clean tree leaves
`git status --porcelain` empty across the repository; the record/disk and cabal/disk
checks pass; temporarily deleting one `other-modules` line from a conformance stanza
makes the run fail naming that component and module, and restoring it passes again;
editing `fold-identity-baseline.golden` to wrong bytes makes `cabal test keiro-dsl-test`
fail, and `update-goldens` restores the committed bytes exactly (git clean afterward).

### Milestone 3: CI idempotence gate, contributor documentation, and closure

This milestone makes the zero-diff property a repository policy and documents the
workflow. `check` mode is `regenerate` with a stricter frame: first verify
`git status --porcelain` is empty over the corpus paths and refuse with a message
naming the `regenerate`-review-commit workflow if not; then regenerate; then fail with
the porcelain list and `git diff --stat` if any byte changed; then run both consistency
checks. Success prints `conformance corpus: ok`.

Add `scripts/check-conformance-corpus.sh` following the conventions of
`scripts/check-extension-policy.sh` (bash, `set -euo pipefail`, `cd` to
`git rev-parse --show-toplevel`, non-zero exit with a labeled message): it runs
`cabal run -v0 keiro-dsl-corpus-regen -- check`. Add two Justfile recipes in the
existing groups: `corpus-regen` (group `haskell`) running the driver's `regenerate`,
and `conformance-corpus-policy` (group `meta`) running the script; append
`conformance-corpus-policy` to the `verify` recipe's dependency list beside
`extension-policy` and `generated-name-policy`. Note in the recipe comment that the
gate re-scaffolds the whole corpus and requires the `keiro-dsl` executable to be built.

Write the contributor documentation. Research found no CONTRIBUTING or developer doc in
the repository (root `README.md` is product-facing; `docs/user/` and `docs/guides/` are
consumer-facing), so create `keiro-dsl/test/README.md` beside the corpus it documents:
what the corpus is, the record-derived manifest model, when and how to run
`corpus-regen` / `update-goldens` / the policy gate, how to add a new conformance suite
(scaffold it once by hand with the public CLI, commit the record, add manifest rows only
if it needs skeleton/workspace/extra-args treatment, extend the cabal stanza, and let
the checker confirm), and the create-once rule that the tool never touches hand-owned
files. Keep the tool's `--help` consistent with it.

Update the changelogs: a root `CHANGELOG.md` Unreleased entry (repository tooling: corpus
regeneration driver, golden accept mode, corpus policy gate) and a
`keiro-dsl/CHANGELOG.md` Unreleased note that the package's test corpus is now
regenerated and policed by `keiro-dsl-corpus-regen` (no library or CLI surface of the
published package changes). Close by running the full validation battery in Concrete
Steps, filling Outcomes & Retrospective, marking the EP-195 rows in
`docs/masterplans/29-stabilize-keiro-dsl-for-wide-adoption.md` complete, and setting the
registry Status there to Complete.


## Concrete Steps

Run every command from `/Users/shinzui/Keikaku/bokuno/keiro`. Transcript excerpts are
indicative; replace them with the real output (and record final wording in Progress) as
implementation proceeds.

Baseline before edits — confirm the corpus and suites are green:

```console
$ nix develop -c cabal build keiro-dsl
...
$ nix develop -c cabal test keiro-dsl-conformance-behavior-complete
Test suite keiro-dsl-conformance-behavior-complete: PASS
$ git status --porcelain -- keiro-dsl/test
$
```

Milestone 1 — build the driver and regenerate one history:

```console
$ nix develop -c cabal build keiro-dsl-corpus-regen
...
$ nix develop -c cabal run -v0 keiro-dsl-corpus-regen -- regenerate --only keiro-dsl/test/conformance-behavior-complete
corpus: 1 of 41 invocations selected
scaffold keiro-dsl/test/fixtures/behavior-complete.keiro --out keiro-dsl/test/conformance-behavior-complete
  Generated/BehaviorComplete/Journey/Transducer.hs (overwritten)
  BehaviorComplete/Journey/BehaviorHoles.hs (skipped: already present)
...
corpus regeneration complete; git reports no changes
$ git status --porcelain -- keiro-dsl/test
$
```

Milestone 1 restore and preservation proofs:

```console
$ sed -i '' '20d' keiro-dsl/test/conformance-behavior-complete/Generated/BehaviorComplete/Journey/Transducer.hs
$ nix develop -c cabal run -v0 keiro-dsl-corpus-regen -- regenerate --only keiro-dsl/test/conformance-behavior-complete
...
$ git status --porcelain -- keiro-dsl/test
$ printf '%s\n' '-- sentinel: hand-owned' >> keiro-dsl/test/conformance-behavior-complete/BehaviorComplete/Journey/BehaviorHoles.hs
$ nix develop -c cabal run -v0 keiro-dsl-corpus-regen -- regenerate --only keiro-dsl/test/conformance-behavior-complete
...
$ git diff --stat -- keiro-dsl/test
 .../BehaviorComplete/Journey/BehaviorHoles.hs | 1 +
$ git checkout -- keiro-dsl/test/conformance-behavior-complete/BehaviorComplete/Journey/BehaviorHoles.hs
```

Milestone 2 — full corpus, checks, goldens:

```console
$ nix develop -c cabal run -v0 keiro-dsl-corpus-regen -- regenerate
corpus: 41 invocations over 34 recorded histories
...
record/disk consistency: ok
cabal inventory consistency: ok (1 exempted module)
corpus regeneration complete; git reports no changes
$ git status --porcelain
$ nix develop -c cabal run -v0 keiro-dsl-corpus-regen -- update-goldens
...
6 golden files verified, 0 rewritten
```

Milestone 3 — gate and closure battery:

```console
$ scripts/check-conformance-corpus.sh
conformance corpus: ok
$ nix develop -c just conformance-corpus-policy
...
conformance corpus: ok
$ nix develop -c cabal test keiro-dsl
...
$ nix develop -c cabal build all
...
$ scripts/check-extension-policy.sh
extension policy: ok
$ scripts/check-generated-name-policy.sh
generated Haskell naming policy: ok
$ git diff --check
$
```

If a full `regenerate` shows drift for any tree, stop: inspect the diff, classify it
(manifest/flag derivation defect versus real generator drift), record it in Surprises &
Discoveries, and only proceed once the tree reproduces byte-identically or the drift is
explicitly owned by another plan.


## Validation and Acceptance

Acceptance is behavioral. All commands run from the repository root on the current
corpus; "clean" means `git status --porcelain` prints nothing for the paths named.

1. Zero-diff idempotence: on a clean tree, `cabal run -v0 keiro-dsl-corpus-regen --
   regenerate` exits zero, its per-module report shows the public CLI's expected
   `(overwritten)`, `(unchanged)`, and `(skipped: already present)` dispositions, and the
   tree is still clean afterward. Single-spec runs overwrite identical bytes; workspace
   runs can report unchanged without writing.
   This simultaneously proves the out-of-scope guarantee that the tool changes no
   generated output byte.
2. Restoration: after deleting one line from a committed generated module (banner
   kept), or deleting the file entirely, regeneration restores it byte-identically
   (`git status --porcelain` clean).
3. Create-once preservation: a sentinel comment appended to
   `keiro-dsl/test/conformance-behavior-complete/BehaviorComplete/Journey/BehaviorHoles.hs`
   survives regeneration; the run reports the file `(skipped: already present)`; the
   sentinel is then reverted by hand.
4. Banner safety: removing the banner line from one generated file makes the affected
   invocation fail with the scaffolder's `MissingGeneratedBanner` refusal; the file is
   not overwritten; the tool exits non-zero naming the invocation. The tool has no
   force path.
5. Golden mode: corrupting `keiro-dsl/test/fixtures/fold-identity-baseline.golden`
   makes `cabal test keiro-dsl-test` fail; `update-goldens` rewrites it to exactly the
   committed bytes (tree clean); a genuine renderer change would instead surface as an
   ordinary reviewable golden diff.
6. Inventory check: deleting one `Generated.*` line from a conformance stanza's
   `other-modules` in `keiro-dsl/keiro-dsl.cabal` makes `check` fail naming that
   component and module; restoring it passes. The exemption list contains only
   reviewed entries (initially the structural `BehaviorContract` module) and removing
   an exemption whose module is still uncompiled fails with the same precision.
7. Gate behavior: `scripts/check-conformance-corpus.sh` on a clean tree prints
   `conformance corpus: ok` and exits zero; with a pre-dirtied corpus path it refuses
   before scaffolding; with clean input but a generator that would change output it
   exits non-zero printing the porcelain list and diff stat. `just verify` now includes
   the gate.
8. Nothing is committed by the tool in any mode (verify `git log` is unchanged after
   every run above), and all 35 corpus test suites still pass:
   `cabal test keiro-dsl` and `cabal test keiro-workspace-proof-conformance` are green.


## Idempotence and Recovery

The whole design is idempotence-first. Regeneration drives the public scaffolder, whose
workspace write path compares bytes and reports `(unchanged)` without writing, whose
single-spec path overwrites with deterministic identical bytes, and whose shared boundary
skips create-once files and refuses bannerless targets before any write. Running
`regenerate` twice is therefore byte-idempotent, and `check` is `regenerate` plus
assertions. `--only` restricts
blast radius during iteration. The tool never deletes files, never edits cabal stanzas,
never forces overwrites, and never commits — every effect it can have is visible in
`git status` and reversible with `git checkout --` of the affected paths.

Order dependence is confined and declared: the eight skeleton invocations must run in
manifest order because they share one record slot (last writer wins); the driver
guarantees this, and a crash mid-sequence is repaired by simply re-running, which
replays all eight. A failed invocation leaves the CLI's partial output in place for
inspection; re-running after fixing the cause converges to the committed state.

The riskiest acceptance steps intentionally corrupt committed files; each is followed by
a regeneration or `git checkout --` restore in Concrete Steps, and none touches
anything outside `keiro-dsl/test` and `keiro-dsl/keiro-dsl.cabal`. There is no
database, network, or wire-format surface anywhere in this plan. If the plan must be
abandoned mid-way, revert the new package directory, manifest, script, Justfile lines,
`cabal.project` line, `test/Main.hs` helper, and doc/changelog edits with an explicit
patch; committed corpus bytes were never legitimately changed, so nothing else needs
rollback.


## Interfaces and Dependencies

The new package `keiro-dsl/tools/corpus-regen/` (`keiro-dsl-corpus-regen.cabal`,
executable `keiro-dsl-corpus-regen`) depends on: `base`, `text`, `containers`,
`directory`, `filepath`, `process` (subprocess control for git, cabal, and the resolved
`keiro-dsl` binary), `Cabal-syntax` (parsing `keiro-dsl/keiro-dsl.cabal`), and the
`keiro-dsl` library for the record parsers. It reuses, and must not fork, these
existing exposed interfaces:

- `Keiro.Dsl.ScaffoldRecord` — single-spec record parsing (spec path, module-root,
  layout, generated/hole rows).
- `Keiro.Dsl.WorkspaceRecord` — workspace record parsing (service, manifest basename,
  layout, module rows with kinds).

Internal module `CorpusPlan` in the new package provides, at the end of Milestone 2
(field and constructor spellings may follow local style; the information content is
fixed):

```haskell
data CorpusEntry
  = SingleSpec { ceSpecPath :: FilePath, ceOutDir :: FilePath
               , ceModuleRoot :: Maybe Text, ceCollocate :: Bool
               , ceExtraArgs :: [Text] }
  | WorkspaceSpec { ceManifestPath :: FilePath, ceOutDir :: FilePath }
  | SkeletonRun { ceKind :: Text, ceRoot :: Text, ceOutDir :: FilePath }

loadCorpusPlan
  :: FilePath            -- repository root
  -> IO (Either [Text] ([CorpusEntry], [FilePath]))
                         -- entries in execution order, exempted generated paths

renderInvocation :: CorpusEntry -> [String]   -- argv for the keiro-dsl binary

checkRecordDiskConsistency :: FilePath -> [CorpusEntry] -> IO [Text]
checkCabalInventory :: FilePath -> [CorpusEntry] -> [FilePath] -> IO [Text]
```

`loadCorpusPlan` merges the supplement manifest
(`keiro-dsl/test/conformance-corpus-manifest.txt`, header
`keiro-dsl conformance corpus manifest v1`; row kinds `workspace`, `skeleton`,
`extra-args`, `uncompiled-generated`, `#` comments) with the parsed committed records,
and fails with precise messages when either side has an unclaimed or contradictory
entry. The two check functions return empty lists on success and human-actionable
failure lines otherwise.

`keiro-dsl/test/Main.hs` gains exactly one new helper,
`assertMatchesGolden :: FilePath -> T.Text -> IO ()`, honoring
`KEIRO_DSL_UPDATE_GOLDENS=1`, used by the six golden sites; no other test interface
changes, and no substring assertion is removed.

External command surface used by the driver: `git rev-parse --show-toplevel`,
`git ls-files`, `git status --porcelain --`, `git diff --stat --`,
`cabal build keiro-dsl exe:keiro-dsl`, `cabal list-bin keiro-dsl`,
`cabal test keiro-dsl-test` (golden mode), and the resolved `keiro-dsl` binary's
public `scaffold` and `new` subcommands with only these flags: `--out`,
`--module-root`, `--collocate`, `--codec-comparison`, `--comparison-out`. The driver
must never pass `--force-generated-overwrite`, `--apply-name-migrations`, or
`--goldens` (no committed history uses them; a future history that does must add its
flags via an `extra-args` row, which makes the exception reviewable).

Repository wiring: one `packages:` line in `cabal.project`; recipes `corpus-regen` and
`conformance-corpus-policy` in `Justfile` with `conformance-corpus-policy` appended to
`verify`; `scripts/check-conformance-corpus.sh` alongside the two existing policy
scripts; contributor documentation in `keiro-dsl/test/README.md`; changelog entries in
`CHANGELOG.md` and `keiro-dsl/CHANGELOG.md`.


## Revision Notes

- 2026-08-05: Corrected the record-derived discovery premise after implementation found
  that 33 authoritative record/manifest pairs were ignored rather than tracked. Milestone
  1 now force-tracks the bounded corpus sidecars, retains the global ignore rule, and
  excludes the stale nested skeleton pair so a clean checkout contains exactly the 34
  histories the driver must replay.
- 2026-08-05: Corrected the idempotence transcript and acceptance language after the
  representative run confirmed that single-spec scaffolds report `(overwritten)` for
  deterministic identical bytes, whereas `(unchanged)` is workspace-only. The behavioral
  acceptance remains an empty tracked diff.

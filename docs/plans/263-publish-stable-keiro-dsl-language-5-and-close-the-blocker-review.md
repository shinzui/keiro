---
id: 263
slug: publish-stable-keiro-dsl-language-5-and-close-the-blocker-review
title: "Publish stable keiro-dsl Language 5 and close the blocker review"
kind: exec-plan
created_at: 2026-08-14T13:33:20Z
intention: "intention_01m0075g1kecjb2959gy704yhc"
master_plan: "docs/masterplans/42-fix-the-final-keiro-release-blockers-and-publish-stable-language-5.md"
---

# Publish stable keiro-dsl Language 5 and close the blocker review

This ExecPlan is a living document. The sections Progress, Surprises & Discoveries,
Decision Log, and Outcomes & Retrospective must be kept up to date as work proceeds.
If durable project context changes, update or create ADRs in docs/adr/ in the same change.


## Purpose / Big Picture

After this change, `language keiro-dsl 5` is the sole published stable and default authoring
contract in the release candidate. Language 4 remains accepted with its original syntax,
runtime semantics, fixtures, and generated bytes under an explicit published-compatibility
lane. The stable Language 5 corpus includes the live workflow allocate/signal/completion proof
from EP-1 in addition to every Language 5-exclusive feature reviewed in REV-6.

This plan is also the blocker-closure gate. It commits the corrected release-candidate state,
performs full adversarial follow-up reviews of the six original subjects, and adds new
`approved` review records only when the evidence warrants that outcome. Any fresh
`changes-requested` result leaves this plan in progress and keeps the release plan blocked.


## Progress

Use a checklist to summarize granular steps. Every stopping point must be documented here,
even if it requires splitting a partially completed task into two ("done" vs. "remaining").
This section must always reflect the actual current state of the work.

- [x] M1: migrate the conformance baseline to explicit per-suite language ownership and preserve Language 4 as published compatibility
- [x] M1: migrate the workflow evolution fixture and its three compiled lanes to Language 5 without weakening the Language 4 parser/corpus proofs
- [x] M2: make Language 5 the sole `Stable`/`PublishedLanguage` registry entry and make Language 4 `CompatibilityOnly`/`PublishedLanguage`
- [x] M2: regenerate the affected corpus, update Language 5 documentation and ADR 16, and pass every DSL/repository gate
- [x] M2: pass ADR validation, all focused Language 5 lanes, and the full 707-example `keiro-dsl-test` suite; prove dirty-tree corpus regeneration is idempotent
- [x] M2: commit the intentional corpus transition, pass the clean-tree corpus policy, and pass fresh-database `just verify` at pre-review SHA `5975ee147a3e4c00fb8eff097bd6c4a2b3add36b`
- [x] M3: stage the release-candidate change with explicit paths; `git diff --cached --name-only` reports exactly:

  ```text
  CHANGELOG.md
  README.md
  docs/adr/0016-source-language-provenance-wraps-the-semantic-keiro-dsl-graph.md
  docs/adr/log.md
  docs/capabilities/typed-projection-catalogs.md
  docs/corpus/keiro-dsl-corpus.md
  docs/guides/README.md
  docs/guides/brownfield-migration-and-transducer-modeling.md
  docs/guides/choosing-a-projection.md
  docs/guides/choosing-keiro-dsl.md
  docs/guides/evolution-and-replayability.md
  docs/guides/project-read-models.md
  docs/masterplans/42-fix-the-final-keiro-release-blockers-and-publish-stable-language-5.md
  docs/plans/263-publish-stable-keiro-dsl-language-5-and-close-the-blocker-review.md
  docs/user/README.md
  docs/user/api-reference.md
  docs/user/command-cycle.md
  docs/user/deploy-ordering.md
  docs/user/mapped-consumer-adoption.md
  docs/user/migration-ownership.md
  docs/user/read-models-and-projections.md
  docs/user/typed-spec-toolchain.md
  docs/user/work-queues.md
  keiro-dsl/CHANGELOG.md
  keiro-dsl/src/Keiro/Dsl/LanguageVersion.hs
  keiro-dsl/test/Keiro/Dsl/ConformanceBaseline.hs
  keiro-dsl/test/Keiro/Dsl/FrontendProfiles.hs
  keiro-dsl/test/Main.hs
  keiro-dsl/test/conformance-aggregate-scalars/keiro-dsl-ledger.context.aggregate-scalars.txt
  keiro-dsl/test/conformance-baseline.json
  keiro-dsl/test/conformance-behavior-complete/keiro-dsl-ledger.context.behavior-complete.txt
  keiro-dsl/test/conformance-coldstart/keiro-dsl-ledger.context.billing.txt
  keiro-dsl/test/conformance-contract/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-declarative-router/keiro-dsl-ledger.context.transfer-routing.txt
  keiro-dsl/test/conformance-dispatch-full/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-domain-outcomes/keiro-dsl-ledger.context.domain-outcomes.txt
  keiro-dsl/test/conformance-import-planning/keiro-dsl-ledger.context.import-planning-collisions.txt
  keiro-dsl/test/conformance-intake-full/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-intake-runtime/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-mapped-queue/keiro-dsl-ledger.context.mapped-queue.txt
  keiro-dsl/test/conformance-mapped-readmodel/keiro-dsl-ledger.context.mapped-readmodel.txt
  keiro-dsl/test/conformance-newsurface/keiro-dsl-ledger.context.transfer-routing.txt
  keiro-dsl/test/conformance-nominal-scalars/keiro-dsl-ledger.context.nominal-scalars.txt
  keiro-dsl/test/conformance-process-full/keiro-dsl-ledger.context.surge-demo.txt
  keiro-dsl/test/conformance-process-runtime/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-process/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-projection-catalog/keiro-dsl-ledger.context.catalog-demo.txt
  keiro-dsl/test/conformance-publisher-runtime/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-queue-runtime/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-queue/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-readmodel-runtime/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-replay/keiro-dsl-ledger.context.replay-divergence.txt
  keiro-dsl/test/conformance-router-full/keiro-dsl-ledger.context.incident-paging.txt
  keiro-dsl/test/conformance-router-runtime/keiro-dsl-ledger.context.incident-paging.txt
  keiro-dsl/test/conformance-router/keiro-dsl-ledger.context.incident-paging.txt
  keiro-dsl/test/conformance-scalar-expressions/keiro-dsl-ledger.context.aggregate-scalar-expressions.txt
  keiro-dsl/test/conformance-service-package/runtime/src/keiro-dsl-ledger.workspace.workspace-proof.txt
  keiro-dsl/test/conformance-snapshot/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-structural/keiro-dsl-ledger.context.structural-conformance.txt
  keiro-dsl/test/conformance-v2/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-workflow-full/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-workflow-runtime/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-workflow/keiro-dsl-ledger.context.hospital-capacity.txt
  keiro-dsl/test/conformance-workspace-nominals/keiro-dsl-ledger.workspace.workspace-nominal-proof.txt
  keiro-dsl/test/conformance/keiro-dsl-ledger.context.hospital-capacity.txt
  ```
- [x] M3: commit the release-candidate code and documentation with MasterPlan, ExecPlan, and Intention trailers; retain the pre-review forward-fix candidate at `5975ee147a3e4c00fb8eff097bd6c4a2b3add36b`
- [ ] M3: run six full adversarial follow-up reviews at that exact commit and allocate new REV handles
- [ ] M3: validate the review bundle and stop on any `changes-requested` outcome
- [ ] M4: record closure evidence and mark the MasterPlan release gate ready for EP-5


## Surprises & Discoveries

Document unexpected behaviors, bugs, optimizations, or insights discovered during
implementation. Provide concise evidence.

- REV-6 found no new blocker in Language 5-exclusive features, but the existing workflow-runtime
  lane is Language 4 and tautological. Fixing it without moving a workflow proof into the stable
  Language 5 lane would leave the inherited surface under-proven at publication.
- `keiro-dsl/test/conformance-baseline.json` currently derives a suite's expected banner from
  the moving `stableLanguageVersion` or `authoringLanguageVersion` based only on its role.
  Flipping the stable pointer alone would demand Language 5 banners from the Language 4 corpus
  and invite a mass rewrite of published evidence.
- The reviews profile models a review as an immutable event. A later full review supersedes an
  earlier one by using the same `subject` and `component` identity at a newer `reviewedSha`;
  there is no status field and no reason to edit REV-1 through REV-6.
- A review record requires a full 40-character commit SHA. Code and registry changes therefore
  need a local release-candidate commit before the closure review is authored; the review
  records themselves land in a later documentation commit.
- Schema 2 and corpus regeneration changed only the intentionally migrated workflow fixture and
  its three pairs of generated workflow modules plus provenance ledgers. The focused baseline
  passed 2 examples, and all three workflow lanes passed, including the live PostgreSQL proof
  that allocates, signals, resumes, and completes with the opaque id.
- Publishing Language 5 exposed two moving-pointer couplings in the full DSL suite. The feature
  diagnostic selected the newest published supporting language and rewrote a frozen Language 4
  diagnostic to version 5; legacy read-model round-trip tests also reparsed Language 4 syntax
  under the moving stable pointer. Diagnostics now retain the latest published compatibility
  owner, while predecessor round trips select Language 4 explicitly. The full 707-example suite
  passes without changing the released golden.
- `keiro-dsl-corpus-regen check` deliberately refuses any dirty corpus path, including the 36
  intended one-line ledger maturity changes in this plan. Running `regenerate --allow-dirty`
  before and after the final source changes produced the same corpus diff SHA-256
  `6e2e6de9408098319523b6e356ec7c4c61d14b9cad10696f579e296c31481590`; every generated module
  reported unchanged. The strict clean-tree policy therefore runs immediately after the
  release-candidate commit rather than treating its pre-commit refusal as drift.
- The first fresh-database repository gate exposed a pre-existing compile-time probe that invoked
  GHC with the unversioned `-package keiro`. The package environment contained both the current
  0.11.0.0 build and EP-3's installed 0.11.0.1 metadata probe, so the test observed an ambiguous
  module instead of the intended `ValidatedEventStream` type error. The forward fix derives the
  exact package selector from `Paths_keiro.version`; its focused 16-example group and the full
  611-example Keiro suite pass at the pre-review candidate.
- The first full follow-up review sweep found current documentation that escaped the publication
  edit: one worked-guide `runWorkflowWith` signature omitted `Error StoreError`, three corpus
  inventory rows and one compiled-lane description still said “candidate,” and the durable-
  execution capability sketch still used the obsolete awakeable/child call shapes. No approval
  record was written against that state. These are forward-fixed before the reviewed SHA is
  finalized, and the documentation-acceptance search is repeated with historical records excluded.


## Decision Log

Record every decision made while working on the plan.

- Decision: Give every compiled primary suite an explicit `languageVersion` in conformance
  baseline schema 2.
  Rationale: Suite ownership must not move when the registry's stable/authoring pointers move.
  Explicit ownership preserves released evidence and makes future publication transitions
  reviewable rather than implicit.
  Date: 2026-08-14
- Decision: Use `stable-primary`, `published-compatibility`, `compatibility-proof`, and
  `version-independent` roles after publication; remove `candidate-primary` from the active
  manifest.
  Rationale: There is no candidate after Language 5 publishes. Language 4 remains a published
  immutable contract even though it is no longer the recommended stable authoring version.
  Date: 2026-08-14
- Decision: Deliberately migrate `test/fixtures/workflow-evolution.keiro` and its workflow,
  workflow-runtime, and workflow-full outputs to Language 5.
  Rationale: This directly closes REV-6's inherited workflow gap in the stable lane while the
  Language 4 skeleton and fixture corpus continue to prove predecessor compatibility.
  Date: 2026-08-14
- Decision: Add six follow-up review records, one per original subject, rather than one broad
  release review.
  Rationale: The reviews profile defines one record as one subject at one commit. Matching the
  original component identities preserves review-chain granularity and makes later incremental
  review possible.
  Date: 2026-08-14
- Decision: Do not assume the follow-up outcomes are approved.
  Rationale: An adversarial review is evidence gathering, not a ceremonial checkbox. Any new
  blocker must stop publication and be fixed before this plan completes.
  Date: 2026-08-14
- Decision: Keep released feature-diagnostic text attached to the latest published compatibility
  owner when a successor becomes stable.
  Rationale: Publication changes the authoring recommendation, not the diagnostic bytes of an
  immutable predecessor. A Language-5-only feature still names Language 5 because it has no
  published compatibility owner.
  Date: 2026-08-14
- Decision: Reparse historical fixture renderings under their explicit recorded language instead
  of `currentStableLanguageVersion`.
  Rationale: A rendered semantic graph does not carry source provenance. Supplying Language 4 in
  the predecessor round-trip test prevents a stable-pointer move from reinterpreting released
  `feed`/`consistency` syntax as Language 5.
  Date: 2026-08-14


## Outcomes & Retrospective

Summarize outcomes, gaps, and lessons learned at major milestones or at completion.
Compare the result against the original purpose. Before marking the plan complete,
distill durable project context from the Decision Log, Surprises & Discoveries, and
this section into docs/adr/. Keep task-local execution details here.

Milestone 2 publishes Language 5 in the registry without rewriting Language 4 behavior. The
schema-2 baseline now owns eight stable Language 5 suites, 27 published-compatibility Language 4
suites, three compatibility proofs, and one version-independent comparison. Corpus regeneration
changes only the maturity row in 36 ledgers; all generated Haskell is unchanged. User guidance,
the corpus inventory, capability and guide references, changelogs, and ADR 16 now describe the
published split. Focused Language 5 suites, the live PostgreSQL workflow proof, strict ADR
validation, and all 707 DSL examples pass. Clean-tree repository verification and the six
commit-pinned adversarial reviews remain before closure. At the pre-review code candidate
`5975ee147a3e4c00fb8eff097bd6c4a2b3add36b`, a fresh database `just verify` run passes the live
Jitsurei demo, every package build and test suite, all 39 corpus invocations, documentation and
policy validation, and the 34-example migration suite. Only the six immutable follow-up reviews
and their validation remain after the review-found documentation corrections are committed and
the exact final candidate is reverified.


## Context and Orientation

`keiro-dsl/src/Keiro/Dsl/LanguageVersion.hs` owns the registry. At the reviewed commit,
Language 4 is `Stable PublishedLanguage`; Language 5 is `Candidate CandidateLanguage`.
`currentStableLanguageVersion` selects the sole `Stable` entry, while
`currentAuthoringLanguageVersion` selects the sole candidate and otherwise falls back to
stable. `keiro-dsl/test/Keiro/Dsl/FrontendProfiles.hs` pins the complete registry, profile,
capability, support, maturity, predecessor, stable, and authoring facts.

`keiro-dsl/test/conformance-baseline.json` and
`keiro-dsl/test/Keiro/Dsl/ConformanceBaseline.hs` govern 245-plus source fixtures and every
compiled `keiro-dsl-conformance-*` Cabal component. Schema 1 assigns generated primary suites a
role of `stable-primary` or `candidate-primary` and derives their expected version from global
pointers. Publication needs schema 2: each primary suite carries its own positive
`languageVersion`; `stable-primary` must equal the current stable version;
`published-compatibility` must name a `PublishedLanguage` entry that is not current stable;
`compatibility-proof` and `version-independent` have no primary generated-language claim.
This keeps version 4 output pinned when version 5 becomes stable.

The focused Language 5 compiled suites already cover typed domain outcomes, mapped queues,
mapped read models, projection catalogs/external reads, and declarative routers. The workflow
source `keiro-dsl/test/fixtures/workflow-evolution.keiro` currently declares Language 4 and
feeds `keiro-dsl-conformance-workflow`, `keiro-dsl-conformance-workflow-runtime`, and
`keiro-dsl-conformance-workflow-full`. Migrate that source deliberately to Language 5 and
regenerate only its attributable outputs after EP-1. Language 4 remains covered by its many
unchanged fixtures, parser compatibility tests, and the generated skeleton corpus.

`docs/corpus/keiro-dsl-corpus.md`, `docs/user/typed-spec-toolchain.md`,
`docs/user/read-models-and-projections.md`, `docs/user/mapped-consumer-adoption.md`, other user
references found by the candidate-language sweep, `keiro-dsl/CHANGELOG.md`, and root
`CHANGELOG.md` describe Language 5 as a candidate. Update current-state prose to stable/published
without rewriting historical dated statements. Do not mass-upgrade consumer examples whose
purpose is Language 4 compatibility.

The assurance bundle is `docs/reviews/`, registered as
`mori://shinzui/keiro/okf/reviews`. Its profile is pinned in `docs/reviews/profile.dhall` to
`mori://shinzui/okf-profiles/profiles/reviews`. It requires one record per subject and a unique
`REV-N`; `okf id next` allocates the next handle. A full follow-up review repeats the original
`subject`, `subjectKind`, and `component`, uses `coverage: full`, and pins the release-candidate
commit in `reviewedSha`. Its body may link the earlier local record, but the earlier file remains
unchanged. Update `index.md` and `log.md`, and validate with strict profile/log enforcement.

[ADR 16](../adr/0016-source-language-provenance-wraps-the-semantic-keiro-dsl-graph.md)
is the controlling decision: registration is not publication, one published stable entry is the
authoring recommendation, and published predecessors are immutable. Amend it with the Language 5
publication and explicit Language 4 corpus ownership. [ADR 20](../adr/0020-service-conformance-packages-import-one-runtime-owned-facade.md)
governs the live workflow conformance proof. EP-1's ADR 24 amendment governs awakeable
compatibility. No cross-repository ADR applies; the OKF profile is a dependency contract, not an
architecture decision, and was inspected through Mori rather than guessed.


## Plan of Work

Milestone 1 makes corpus ownership explicit before moving a pointer. Bump the baseline schema
identifier to `keiro-dsl/conformance-baseline/2`. Add `suiteLanguageVersion :: Maybe Natural` to
`CompiledSuite` and parse `languageVersion`. During this preparatory milestone, retain the current
roles but require explicit ownership: every `stable-primary` row names the current stable version
4, every `candidate-primary` row names the current authoring candidate version 5, and
compatibility/version-independent rows omit it. Use the row's explicit version for source
observation, generated-banner, and module-inventory checks. Migrate
`workflow-evolution.keiro` to Language 5, regenerate its three compiled lanes, and mark those
rows `candidate-primary` version 5. This milestone is independently green while the registry
still says Language 4 stable and proves schema 2 can pin both lanes without rewriting unrelated
generated files.

Milestone 2 performs the publication transition atomically with role reclassification. Convert
all Language 5 `candidate-primary` rows to `stable-primary` version 5, convert unchanged
Language 4 `stable-primary` rows to `published-compatibility` version 4, remove
`candidate-primary` as an allowed active role, and require a published-compatibility version to
be a published non-stable registry entry. In
`LanguageVersion.hs`, change Language 4 to `CompatibilityOnly PublishedLanguage` and Language 5
to `Stable PublishedLanguage`. Update `FrontendProfiles.hs` and every current-state assertion so
both current stable and authoring versions are 5, there is no candidate entry, and all five
registry maturities are published. Regenerate attributable Language 5 output, run corpus
zero-drift checks, and update user-facing Language 5 maturity prose. Amend ADR 16 and its OKF log
entry. The transition must not change syntax profiles, runtime profile identifiers, predecessor
links, feature gates, diagnostics, canonical rendering, or a Language 4 generated byte.

Milestone 3 creates a reviewable commit, then reviews it. Finish EP-1 through EP-3 living
sections, update this plan and the MasterPlan, and create a local Conventional Commit containing
the corrected code, docs, registry, corpus, changelog bullets, and ADR updates with all three
trailers required by this initiative. Record `git rev-parse HEAD`. Against that exact SHA, perform
six full adversarial reviews using the original dimensions and component identities: awakeable
API (REV-1 subject), DSL workflow conformance (REV-2), Jitsurei workflow (REV-3), durable-workflow
docs (REV-4), Keiro version API (REV-5), and stable Language 5 (REV-6). Allocate a fresh handle
for each record. Each review re-reads the whole component, runs its focused reproducer and
regression tests, and records the actual outcome. For a model review, record the actual provider,
model, effort, harness actor, and completion time. If any outcome is `changes-requested`, stop,
record the evidence in this plan and parent, and return to the responsible plan; do not flip it
to approved by editing prose.

Milestone 4 closes the gate only if all six new records are approved. Add each record to
`docs/reviews/index.md` and append one `Addition` entry per record to `docs/reviews/log.md` using
`okf log add`. Run strict review validation and `just verify` once more. Commit the review bundle
and living-plan updates with the required trailers. Mark EP-4 complete in the MasterPlan and
hand EP-5 the reviewed code SHA plus review-record commit SHA.


## Concrete Steps

Run from `/Users/shinzui/Keikaku/bokuno/keiro`. Establish the current registry and baseline:

```bash
cabal test keiro-dsl-test --test-options='--match "FrontendProfiles"'
cabal test keiro-dsl-test --test-options='--match "conformance baseline"'
rg -n 'Candidate|CandidateLanguage|currentStableLanguageVersion|currentAuthoringLanguageVersion' keiro-dsl/src/Keiro/Dsl/LanguageVersion.hs keiro-dsl/test
```

After schema and fixture edits, regenerate and run all focused stable-Language-5 lanes:

```bash
just corpus-regen
cabal test keiro-dsl-test
cabal test keiro-dsl-conformance-domain-outcomes
cabal test keiro-dsl-conformance-mapped-queue
cabal test keiro-dsl-conformance-mapped-readmodel
cabal test keiro-dsl-conformance-projection-catalog
cabal test keiro-dsl-conformance-declarative-router
cabal test keiro-dsl-conformance-workflow-runtime
just conformance-corpus-policy
```

Check registry and corpus invariants:

```bash
rg -n 'LanguageDefinition version[1-5]' keiro-dsl/src/Keiro/Dsl/LanguageVersion.hs
rg -n 'candidate-primary|"stableLanguageVersion"|"authoringLanguageVersion"|"languageVersion"' keiro-dsl/test/conformance-baseline.json
git diff --check
```

The registry has one `Stable PublishedLanguage` entry at version 5, no candidate entry, and a
`CompatibilityOnly PublishedLanguage` version 4. The baseline has stable/authoring 5, no
`candidate-primary`, explicit version ownership, and unchanged Language 4 outputs in the
published-compatibility lane.

Validate ADR and repository state, update living documents, then commit the release candidate
with the exact trailers shown here:

```bash
okf validate docs/adr --strict --profile docs/adr/profile.dhall --profile-enforce --log-enforce
just verify
git status --short
git commit
git rev-parse HEAD
```

Before `git commit`, stage each intended path from `git status --short` with an explicit
`git add <exact-path>` invocation and copy the resulting path list into Progress. Do not use
`git add -A`, `git add .`, or a broad directory in a dirty worktree; unrelated changes belong to
the user and must remain unstaged.

The Conventional Commit message includes:

```text
MasterPlan: docs/masterplans/42-fix-the-final-keiro-release-blockers-and-publish-stable-language-5.md
ExecPlan: docs/plans/263-publish-stable-keiro-dsl-language-5-and-close-the-blocker-review.md
Intention: intention_01m0075g1kecjb2959gy704yhc
```

Before each follow-up record, allocate rather than guess its handle:

```bash
okf id list docs/reviews --profile docs/reviews/profile.dhall
okf id next docs/reviews --profile docs/reviews/profile.dhall REV
```

After writing each record, append its log entry with the actual handle and a concrete message.
For example, if the newly allocated awakeable follow-up handle is in `review_id`:

```bash
okf log add docs/reviews "$review_id" --kind Addition --message 'Follow-up awakeable API review records the post-fix outcome.'
```

The command defaults to the actual UTC date. Use an equally specific message for the other five
subjects. Validate after every record and at the end:

```bash
okf validate docs/reviews --strict --profile docs/reviews/profile.dhall --profile-enforce --log-enforce
just reviews-validate
just verify
```

If all six outcomes are approved, commit the review records and plan bookkeeping with the same
three trailers. If any outcome requests changes, do not make this closure commit as though the
gate passed; preserve the record, update Progress/Surprises, and resume the relevant fix.


## Validation and Acceptance

Registry acceptance means `currentStableLanguageVersion == version 5`,
`currentAuthoringLanguageVersion == version 5`, version 5 is the only `Stable` entry and is
`PublishedLanguage`, version 4 remains `PublishedLanguage` but is `CompatibilityOnly`, and no
candidate exists. Languages 1 through 4 retain their exact profile/predecessor/runtime facts.

Corpus acceptance means every generated-primary suite declares its language owner explicitly.
Every stable-primary source/banner is Language 5. Every published-compatibility source/banner
remains at its recorded predecessor version. The Language 4 generated diff is empty except for
manifest/accounting prose; no mass banner rewrite is accepted. The stable Language 5 set includes
the six feature targets from REV-6 plus the real PostgreSQL workflow-runtime completion proof.
All core and compiled suites and `just conformance-corpus-policy` pass.

Documentation acceptance means no current-state user documentation calls Language 5 a candidate
or Language 4 the current stable contract. Historical changelog/review/plan statements retain
their dates and meaning. ADR 16 records the publication transition and predecessor-evidence rule.

Review acceptance means six new valid Review concepts name the same six subject/component
identities at the exact release-candidate SHA. Each body contains focused evidence and an honest
outcome. EP-4 completes only if every outcome is `approved`, strict profile/log validation and
`just verify` pass, and the original REV-1 through REV-6 files are byte-identical.


## Idempotence and Recovery

Corpus generation and all tests are deterministic and safe to repeat. Keep the registry flip,
baseline-role migration, and attributable regeneration in one release-candidate commit so an
intermediate inconsistent state is never presented as complete. If generation changes an
unrelated Language 4 file, stop and inspect why; do not accept broad churn merely to make the
baseline pass.

Review IDs are never recycled. If a drafted record is abandoned before commit, retain or remove
the uncommitted file deliberately, then ask `okf id next` again; never fill a historical gap.
Once a review record is committed, correct material errors with a new record rather than changing
its outcome or reviewed SHA. If the publication gate fails after the registry commit, add a
forward fix commit; do not rewrite or force-push reviewed history.


## Interfaces and Dependencies

The final registry rows are conceptually:

```haskell
LanguageDefinition version4 (Just version3) LanguageBodyParserV2 profileV3 runtimeProfileV3 CompatibilityOnly PublishedLanguage
LanguageDefinition version5 (Just version4) LanguageBodyParserV2 profileV4 runtimeProfileV4 Stable PublishedLanguage
```

Do not change `profileV3`, `profileV4`, `runtimeProfileV3`, or `runtimeProfileV4` in this plan.

The baseline's compiled-suite model gains explicit ownership:

```haskell
data CompiledSuite = CompiledSuite
  { suiteComponent :: Text
  , suiteDirectory :: FilePath
  , suiteSource :: Maybe FilePath
  , suiteGeneration :: Text
  , suiteRole :: Text
  , suiteLanguageVersion :: Maybe Natural
  , suiteReason :: Text
  }
```

Stable and published-compatibility rows require `suiteLanguageVersion`; compatibility-proof and
version-independent rows omit it. The checker consults `languageRegistry` to prove maturity and
support, rather than treating any positive number as published.

The review profile remains the pinned `docs/reviews/profile.dhall`; do not upgrade or fork it as
part of blocker closure. The six follow-up records reuse the original `subject`, `subjectKind`,
and `component`, pin the new SHA, use `coverage: full`, and record actual reviewer metadata and
dimensions. Use only local Review links in prose and canonical `mori://` URIs in frontmatter.

This plan consumes the completed outputs of plans 260, 261, and 262. EP-5 consumes this plan's
release-candidate SHA and six approved follow-up records; it may change only release metadata and
must rerun the complete gate before publishing.
